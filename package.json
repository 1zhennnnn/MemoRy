{
  "name": "memory",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "typecheck": "tsc --build lib/db lib/api-zod && pnpm --filter @workspace/api-server exec tsc --noEmit",
    "typecheck:libs": "tsc --build lib/db lib/api-zod",
    "build": "tsc --build lib/db lib/api-zod && pnpm --filter @workspace/api-server run build",
    "dev": "pnpm --filter @workspace/api-server run dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
